node_modules
dist
dist-ssr
*.local
.vercel
.DS_Store
.env*

# Legacy pre-Codex dashboard wireframes (reference only, missing shared
# stylesheets, not part of the shippable prototype) — see home.html port
# into DashboardHomePage in src/CheckoutLinkApp.jsx.
phase-2-checkout-links/
